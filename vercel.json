{
  "build": {
    "env": {
      "NODE_OPTIONS": "--openssl-legacy-provider"
    },
    "nodeVersion": "16.x"
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}